name: ui_napses_assignment
description: A Project for UI Assignment

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  custom_navigation_bar: ^0.8.1
  styled_widget: ^0.3.1
  flutter_rounded_date_picker: ^2.0.2
  intl: ^0.17.0
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
